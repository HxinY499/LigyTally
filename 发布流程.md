# Ligy Tally 发布流程

## 仓库与安装包

- 私有源码仓库：`HxinY499/LigyTally`
- 安装包：阿里云 OSS 桶 `ligy-tally-releases`（杭州，标准存储，本地冗余，公共读）

源码只推送到私有仓库。APK、SHA-256 和 `latest.json` 只上传到 OSS。
不要把 AccessKey、`~/.ossutilconfig` 或密钥写进仓库、应用或本文件的可复制命令。

应用启动后请求：

```text
https://ligy-tally-releases.oss-cn-hangzhou.aliyuncs.com/latest.json
```

清单里的 `body` 会原样进更新浮层。应用下载安装包用的是清单里的 `apk_url`，
不是桶上那条默认的 `LigyTally-latest.apk`。

阿里云默认公网域名不能匿名下发 APK（`ApkDownloadForbidden`）。
`ligezhang.cn` 备案完成并绑到桶之前，`apk_url` 走 GitHub Releases：

```text
https://github.com/HxinY499/LigyTally-Releases/releases/download/v1.0.1/LigyTally-1.0.1.apk
```

不要把 `https://ligy-tally-releases.oss-cn-hangzhou.aliyuncs.com/LigyTally-latest.apk`
发给用户，这条现在会 400。首次安装用清单里的 `apk_url`。

旧版（1.5.9 及更早）仍向 GitHub Releases 查更新。在改回自定义域名之前，
每次发版也要把 APK 发到 `HxinY499/LigyTally-Releases`，并让清单里的
`apk_url` 指向该 Release 资产。

## 发布流程

### 1. 修改版本号

修改 `pubspec.yaml`：

```yaml
version: 1.0.1+2
```

- `1.0.1` 是用户看到的版本号。
- `+2` 是 Android versionCode，每次必须递增，不能重复或降低。

用下面任一方式确认版本号没被用过：

```bash
curl -sS https://ligy-tally-releases.oss-cn-hangzhou.aliyuncs.com/latest.json
ossutil ls oss://ligy-tally-releases
```

### 2. 构建正式 APK

如果 shell 里配了 pub 镜像，构建前先取消，否则 `pubspec.lock` 里的依赖源
会被改写成镜像地址并混进提交：

```bash
unset PUB_HOSTED_URL FLUTTER_STORAGE_BASE_URL
./scripts/build_release.sh
```

脚本会自动运行分析、测试、正式签名、验签和隐私检查。输出文件：

```text
dist/LigyTally-1.0.1.apk
dist/LigyTally-1.0.1.apk.sha256
```

同时会在仓库外生成调试符号：

```text
~/Documents/LigyTally-release-symbols/1.0.1/
```

**这个目录要长期保留并自己备份。** 发布版做了代码混淆，没有对应版本的
符号文件，线上崩溃日志无法还原成可读的代码位置。它不进Git，删掉就没了。

### 3. 核对构建结果

脚本跑完不要只看“成功”，确认这几项：

- `apksigner verify` 输出 `Verifies`。
- 证书 SHA-256 是
  `0094e3f2217ee3e700911b640dc44a92374abc6ed4bc9d9d03d5afa65a28a94d`。
  指纹不一致说明用错了密钥，这个包不能发，已安装用户无法覆盖升级。
- `dist/*.apk.sha256` 里只有文件名，没有本机绝对路径。

复算一遍校验值：

```bash
cd dist && shasum -a 256 -c LigyTally-1.0.1.apk.sha256
```

### 4. 提交源码

先看清楚改了什么，确认没有 JKS、密码、数据库、备份或其他私人文件

按文件提交，不要用 `git add -A`，避免把临时文件或私人文件一起带进去：

```bash
git add pubspec.yaml lib/
git commit -m "Release 1.0.1"
git push origin main
```

如果 `pubspec.lock` 出现大量 `pub.flutter-io.cn` 之类的镜像地址，那是镜像
环境变量造成的，先`git checkout pubspec.lock` 还原，不要提交。

### 5. 发布到 OSS

`latest.json` 的 `body` 会原样写进更新浮层，应用启动检测到新版本后
**直接展示这段文字里的条目**。所以 notes 是给用户看的。

必须用下面这个结构，标题固定，每条以 `- ` 开头。类型写在破折号后面、
正文前面，和正文用一个空格分开。浮层会把这三个词收成图标，用户看不到
「功能 / 修复 / 优化」这几个字：

```text
## 更新内容

- 功能 记账时可记录位置，点一下获取当前地点
- 修复 统计页金额在窄屏上不再折行
- 优化 外观页的预置风格改成适中密度
```

三个类型：

- `功能`：用户以前做不到的事
- `修复`：以前坏了，现在好了
- `优化`：原来能用，现在更好用或更好看

对不上这三类的行不要硬套，不加前缀，浮层仍画圆点。不要用 emoji，
也不要再加「新功能」「Bug 修复」这种小标题。

要求：

- 第一行必须是 `## 更新内容`，不要改标题，也不要再加别的一、二级标题。
- 每条一行，以 `- ` 开头。一条只写一件用户能看见或用到的事。
- 用中文完整句子。不要写内部重构、依赖升级、修测试这类用户无感的内容。
- 不要嵌套列表、不要贴链接、不要贴截图、不要写安装步骤。
- 不要在 notes 里重复版本号，浮层标题里已经有。
- 3–6 条为宜。再长浮层里会滚动，但用户不会逐条细读。

发之前，先按这个格式写出本次文案，交给人看。
没确认可以用之前，不要执行 `./scripts/publish_release.sh`。
如果已经明确说「不用确认」，跳过这一步，直接按格式生成文案并用于发布。

```bash
./scripts/publish_release.sh "$(cat <<'EOF'
## 更新内容

- 第一条用户能直接感知的变化
- 第二条
EOF
)"
```

脚本会上传带版本号的 APK、覆盖 `LigyTally-latest.apk`、上传 `.sha256`，
最后覆盖 `latest.json`。旧版本的 APK 留在桶里，方便回滚。

`publish_release.sh` 会把 `apk_url` 写成 OSS 默认域名。备案完成前不要直接
用这个地址给用户下包：发完后按第 6 步改清单并验证。备案完成后，把
`apk_url` 改成 `https://releases.ligezhang.cn/LigyTally-x.y.z.apk`。

### 6. 验证应用内更新

每次发版后必须做。只看 `latest.json` 的 `tag_name` 不够——1.5.11 就是
清单正常、APK 被阿里云拦了，用户一点「更新」就失败。

不必真的在手机上走完安装。应用内更新能成功，前提是清单里的 `apk_url`
能被匿名 GET 到**完整安装包**。打开浏览器看一眼也不够：OSS 对 APK
返回的是 400 加一段 XML，页面上看不出是包。

验的是清单里的 `apk_url`，不要自己拼 OSS 路径，也不要只验
`LigyTally-latest.apk`。GitHub 会 302，请求必须跟随跳转。

```bash
python3 - <<'PY'
import hashlib, json, urllib.request

manifest = "https://ligy-tally-releases.oss-cn-hangzhou.aliyuncs.com/latest.json"
with urllib.request.urlopen(manifest) as response:
    info = json.load(response)

url = info["apk_url"]
print("apk_url:", url)

with urllib.request.urlopen(url) as response:
    data = response.read()
    print("http:", response.status, "bytes:", len(data), "expected:", info["apk_size"])

digest = hashlib.sha256(data).hexdigest()
print("sha256:", digest)
if response.status != 200:
    raise SystemExit("最终状态不是 200，应用内更新会失败")
if len(data) != int(info["apk_size"]):
    raise SystemExit("体积和清单对不上")
if digest != info["sha256"].lower():
    raise SystemExit("SHA-256 和清单对不上")
print("OK")
PY
```

任一项失败都不要当发布完成。这条命令能抓住「源站拒下、文件缺失、
下到的是错误页、校验值和包不一致」。装不上、权限弹窗这类还是要真机。

## 必须保持不变

- 包名：`com.ligy.ligy_tally`
- 签名密钥：`~/.android/ligy-tally-release.jks`
- Key alias：`ligy-tally`
- 证书 SHA-256：
  `0094E3F2217EE3E700911B640DC44A92374ABC6ED4BC9D9D03D5AFA65A28A94D`
- OSS 桶：`ligy-tally-releases`（杭州，公共读、私有写）
- 更新清单：`https://ligy-tally-releases.oss-cn-hangzhou.aliyuncs.com/latest.json`

签名密钥或密码丢失后，无法给已安装用户发布覆盖升级。
OSS 的 AccessKey 只放本机 `~/.ossutilconfig`，权限用 RAM 子用户
`lotsoss` 的 `AliyunOSSFullAccess`，不要写进应用。

桶必须保持「阻止公共访问」为关。打开后公共读会失效，应用检查更新会静默失败。

## 构建脚本的环境依赖

`scripts/build_release.sh` 里有两处写死的路径，换电脑或升级工具后可能失效：

- `JAVA_HOME` 指向 `/Applications/Android Studio.app/Contents/jbr/Contents/Home`，
  依赖 Android Studio 装在标准位置。
- `apksigner` 指向 `build-tools/36.0.0/`。如果本机没装这个版本，
  用 `sdkmanager "build-tools;36.0.0"` 补装，或同步修改脚本里的版本号。

`scripts/publish_release.sh` 依赖：

- `ossutil` 2.x（当前装在 `/opt/homebrew/bin/ossutil`）
- 本机已执行过 `ossutil config`，配置在 `~/.ossutilconfig`

脚本使用 `set -euo pipefail`，任何一步失败都会立即中断，不会产出半成品 APK。
构建在 `/tmp` 的临时目录里进行，这是为了避免把本机家目录路径写进二进制，
不要改成在仓库目录里直接构建。
