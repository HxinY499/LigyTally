# Ligy Tally 发布流程

## 仓库

- 私有源码仓库：`HxinY499/LigyTally`
- 公开安装包仓库：`HxinY499/LigyTally-Releases`

源码只推送到私有仓库。APK 和 SHA-256 文件只上传到公开仓库的 GitHub Release，
不要把 JKS、签名密码、数据库或用户备份上传到任何仓库。

## 首次在新电脑上发布前

确认签名身份已就绪，否则 `build_release.sh` 会在第一步直接退出：

```bash
ls -l ~/.android/ligy-tally-release.jks
security find-generic-password -a HxinY499 -s "LigyTally Android Release Keystore" >/dev/null && echo "Keychain OK"
gh auth status
```

任一项缺失，先执行签名迁移包里的安装脚本，并用 `gh auth login` 登录。
`gh auth login` 登录成功但没装签名，仍然无法构建——两者都是必需的。

## 每次发布

### 1. 修改版本号

修改 `pubspec.yaml`：

```yaml
version: 1.0.1+2
```

- `1.0.1` 是用户看到的版本号。
- `+2` 是 Android versionCode，每次必须递增，不能重复或降低。

用`git tag -l` 或看公开仓库的 Release 列表确认版本号没被用过。

### 2. 构建正式 APK

如果 shell 里配了 pub 镜像，构建前先取消，否则 `pubspec.lock` 里的依赖源
会被改写成镜像地址并混进提交：

```bash
unset PUB_HOSTED_URL FLUTTER_STORAGE_BASE_URL
./scripts/build_release.sh
```

脚本会自动运行分析、测试、正式签名、验签和隐私检查。输出文件：

```text
dist/LigyTally-1.0.1.apk
dist/LigyTally-1.0.1.apk.sha256
```

同时会在仓库外生成调试符号：

```text
~/Documents/LigyTally-release-symbols/1.0.1/
```

**这个目录要长期保留并自己备份。** 发布版做了代码混淆，没有对应版本的
符号文件，线上崩溃日志无法还原成可读的代码位置。它不进Git，删掉就没了。

### 3. 核对构建结果

脚本跑完不要只看“成功”，确认这几项：

- `apksigner verify` 输出 `Verifies`。
- 证书 SHA-256 是
  `0094e3f2217ee3e700911b640dc44a92374abc6ed4bc9d9d03d5afa65a28a94d`。
  指纹不一致说明用错了密钥，这个包不能发，已安装用户无法覆盖升级。
- `dist/*.apk.sha256` 里只有文件名，没有本机绝对路径。

复算一遍校验值：

```bash
cd dist && shasum -a 256 -c LigyTally-1.0.1.apk.sha256
```

### 4. 提交源码

先看清楚改了什么，确认没有 JKS、密码、数据库、备份或其他私人文件：

```bash
git status
git diff --stat
```

按文件提交，不要用 `git add -A`，避免把临时文件或私人文件一起带进去：

```bash
git add pubspec.yaml lib/
git commit -m "Release 1.0.1"
git push origin main
```

如果 `pubspec.lock` 出现大量 `pub.flutter-io.cn` 之类的镜像地址，那是镜像
环境变量造成的，先`git checkout pubspec.lock` 还原，不要提交。

### 5. 发布到公开仓库

```bash
gh release create v1.0.1 \
  dist/LigyTally-1.0.1.apk \
  dist/LigyTally-1.0.1.apk.sha256 \
  --repo HxinY499/LigyTally-Releases \
  --title "Ligy Tally 1.0.1" \
  --notes "填写本次更新内容"
```

### 6. 验证下载

不要只看网页能打开，把包下回来核对哈希，确认上传没有损坏：

```bash
gh release download v1.0.1 --repo HxinY499/LigyTally-Releases \
  --pattern "*.apk" --output /tmp/verify.apk --clobber
shasum -a 256 /tmp/verify.apk
rm /tmp/verify.apk
```

哈希应与 `dist/LigyTally-1.0.1.apk.sha256` 一致。也可直接打开：

```text
https://github.com/HxinY499/LigyTally-Releases/releases/latest
```

安装新版本时直接覆盖旧版本，不要卸载，否则本地账单和图片会被删除。

## 必须保持不变

- 包名：`com.ligy.ligy_tally`
- 签名密钥：`~/.android/ligy-tally-release.jks`
- Key alias：`ligy-tally`
- 证书 SHA-256：
  `0094E3F2217EE3E700911B640DC44A92374ABC6ED4BC9D9D03D5AFA65A28A94D`

签名密钥或密码丢失后，无法给已安装用户发布覆盖升级。

## 构建脚本的环境依赖

`scripts/build_release.sh` 里有两处写死的路径，换电脑或升级工具后可能失效：

- `JAVA_HOME` 指向 `/Applications/Android Studio.app/Contents/jbr/Contents/Home`，
  依赖 Android Studio 装在标准位置。
- `apksigner` 指向 `build-tools/36.0.0/`。如果本机没装这个版本，
  用 `sdkmanager "build-tools;36.0.0"` 补装，或同步修改脚本里的版本号。

脚本使用 `set -euo pipefail`，任何一步失败都会立即中断，不会产出半成品 APK。
它在 `/tmp` 的临时目录里构建，这是为了避免把本机家目录路径写进二进制，
不要改成在仓库目录里直接构建。

## 安全须知

- 不要为了省事把签名密码写进文件长期留存。如果通过聊天工具传过含明文密码
  的迁移包，装完立刻删掉整个文件夹和压缩包。
- 密码正确的存放位置是 macOS Keychain，`build_release.sh` 会自己去取，
  不需要手动输入或写进脚本。
- 不要把密码贴进任何 AI 对话、终端命令行参数或提交信息。
