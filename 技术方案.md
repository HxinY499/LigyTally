# Ligy Tally 技术方案

## 1. 文档信息

- 产品名称：Ligy Tally
- 技术方案状态：MVP 草案
- 更新日期：2026-08-08
- 目标：实现无账号、无服务端、可离线使用的手机端记账应用

## 2. 技术决策摘要

| 模块 | 选择 |
| --- | --- |
| 客户端框架 | Flutter |
| 开发语言 | Dart |
| 状态管理 | Riverpod |
| 本地数据库 | SQLite + Drift |
| 图表 | fl_chart |
| 相机与相册 | image_picker |
| 图片压缩 | flutter_image_compress |
| 应用目录 | path_provider |
| 文件导入 | file_picker |
| 系统分享 | share_plus |
| 备份打包 | archive |
| 备份加密 | cryptography 或经评估的等价实现 |
| 唯一标识 | UUID |

依赖的具体版本在开始开发时统一锁定当前稳定版，不在方案阶段写死版本号。

## 3. 为什么选择 Flutter

Flutter 与 React Native 都能满足性能和功能需求。本项目选择 Flutter 的依据是：

1. 项目是独立移动端应用，没有已有 React、TypeScript 或 Web 代码需要复用。
2. 未来可能支持 iOS 和 Android，需要尽量复用 UI 与业务代码。
3. 应用主要由表单、长列表、统计图表、图片和本地文件操作构成，符合 Flutter 的能力范围。
4. Drift 可以提供类型安全的 SQLite 查询、迁移和响应式数据监听。
5. Flutter 在双端 UI、图表和手势表现上更容易保持一致。
6. 项目规模较小，减少 JavaScript 与原生模块边界有利于长期维护。

选择 Flutter 不是因为参考应用使用 Flutter，也不是因为 React Native 无法实现。

如果开发者已经具备显著更强的 React Native 实战经验，重新评估人力成本是合理的；当前方案基于“无既定跨端技术栈”的前提。

## 4. 架构原则

### 4.1 总体架构

```text
Flutter UI
  ↓
Riverpod 状态与用例
  ↓
Repository
  ├── Drift / SQLite
  ├── ImageStorage
  └── BackupService
```

### 4.2 约束

- 不建设服务端。
- 不引入网络层和云同步抽象。
- 不为未来不确定需求提前实现账号、冲突合并或同步状态机。
- UI 不直接操作 SQLite 或文件系统。
- 金额、日期、统计和文件路径规则集中管理。
- 文件系统和数据库的一致性必须由业务用例显式保证。

### 4.3 目录结构

```text
lib/
  app/
    app.dart
    theme.dart
    navigation.dart
  core/
    database/
    media/
    backup/
    errors/
    utils/
  features/
    transactions/
      data/
      domain/
      presentation/
    categories/
    statistics/
    settings/
  main.dart
```

采用按功能组织的模块结构，但不强制每个简单页面创建大量空接口。抽象只用于数据库、图片和备份等存在真实边界的模块。

## 5. 页面与状态组织

### 5.1 页面

- 明细页。
- 新增/编辑账单页。
- 账单详情及图片预览页。
- 统计页。
- 分类管理页。
- 导出、备份与恢复页。
- 设置页。

底部使用三个主入口：明细、统计、设置。MVP 路由较少，优先使用 Flutter Navigator；没有深链需求前不必额外引入复杂路由框架。

### 5.2 Riverpod 使用边界

- 页面临时输入状态保留在 Widget 或表单 Controller 中。
- 数据库列表使用 StreamProvider 或等价响应式 Provider。
- 新增、编辑、删除、备份和恢复使用明确的 Notifier/用例方法。
- 不创建一个包含全应用所有状态的巨大 Store。
- 统计条件和统计结果分别建模，避免 UI 自行拼装 SQL 参数。

## 6. 数据库设计

### 6.1 transactions

```sql
CREATE TABLE transactions (
  id TEXT PRIMARY KEY,
  type INTEGER NOT NULL,
  amount_cents INTEGER NOT NULL CHECK (amount_cents > 0),
  category_id TEXT NOT NULL,
  accounting_date TEXT NOT NULL,
  occurred_at INTEGER NOT NULL,
  note TEXT NOT NULL DEFAULT '',
  created_at INTEGER NOT NULL,
  updated_at INTEGER NOT NULL,
  FOREIGN KEY (category_id) REFERENCES categories(id)
);
```

字段说明：

- `type`：0 表示支出，1 表示收入。
- `amount_cents`：整数分，禁止使用浮点数保存金额。
- `accounting_date`：`YYYY-MM-DD` 形式的业务日期。
- `occurred_at`：Unix 毫秒时间戳，用于同一天内排序和展示时间。

单独保存业务日期是为了避免设备跨时区后账单被统计到前一天或后一天。

### 6.2 categories

```sql
CREATE TABLE categories (
  id TEXT PRIMARY KEY,
  type INTEGER NOT NULL,
  name TEXT NOT NULL,
  icon TEXT NOT NULL,
  parent_id TEXT,
  level INTEGER NOT NULL DEFAULT 1,
  sort_order INTEGER NOT NULL DEFAULT 0,
  is_active INTEGER NOT NULL DEFAULT 1,
  created_at INTEGER NOT NULL,
  updated_at INTEGER NOT NULL
);
```

分类停用后保留记录，历史账单继续引用原分类。

分类支持两级树结构。`parent_id` 为空表示一级分类，二级分类通过
`parent_id` 关联一级分类；统计默认向一级分类归集，明细保留实际选择的
一级或二级分类。

默认分类是产品内置的通用配置，不复制外部应用或某个用户的分类数据。
用户分类是本地可修改配置，支持一级/二级新增、停用、删除及 JSON 导入导出。
未被账单引用的分类可物理删除；已被引用的分类只能停用，以维持历史账单的
引用完整性。

分类配置 JSON 使用嵌套结构，不导出内部数据库 ID：

```json
{
  "format": "ligy-tally-categories",
  "version": 1,
  "expense": [
    {
      "name": "餐饮",
      "icon": "restaurant",
      "children": [
        {"name": "三餐", "icon": "meal"}
      ]
    }
  ],
  "income": [
    {"name": "工资", "icon": "salary", "children": []}
  ]
}
```

覆盖导入时，当前分类先停用，再写入新配置；未被账单引用的旧分类清理，
被历史账单引用的旧分类及其父节点保留为停用状态。

### 6.3 transaction_images

```sql
CREATE TABLE transaction_images (
  id TEXT PRIMARY KEY,
  transaction_id TEXT NOT NULL,
  image_path TEXT NOT NULL,
  thumbnail_path TEXT NOT NULL,
  mime_type TEXT NOT NULL,
  width INTEGER NOT NULL,
  height INTEGER NOT NULL,
  size_bytes INTEGER NOT NULL,
  sort_order INTEGER NOT NULL DEFAULT 0,
  created_at INTEGER NOT NULL,
  FOREIGN KEY (transaction_id)
    REFERENCES transactions(id) ON DELETE CASCADE
);
```

数据库只保存应用私有目录下的相对路径，禁止保存绝对沙盒路径。iOS 或 Android 在升级、恢复后可能改变应用根路径。

### 6.4 settings

```sql
CREATE TABLE settings (
  key TEXT PRIMARY KEY,
  value TEXT NOT NULL
);
```

### 6.5 索引

```sql
CREATE INDEX idx_transactions_date
ON transactions(accounting_date);

CREATE INDEX idx_transactions_type_date
ON transactions(type, accounting_date);

CREATE INDEX idx_transactions_category_date
ON transactions(category_id, accounting_date);

CREATE INDEX idx_images_transaction
ON transaction_images(transaction_id, sort_order);
```

### 6.6 数据库迁移

- 使用 Drift 的 `schemaVersion` 和迁移回调。
- 每次结构变更必须提供升级测试。
- 禁止在生产代码中通过删除数据库解决迁移失败。
- 默认分类初始化必须幂等，不能在升级后重复插入。
- 启用 `PRAGMA foreign_keys = ON`。

## 7. 金额与日期

### 7.1 金额

- 用户输入的元转换为整数分后保存。
- 金额解析集中在 Money 工具或值对象中。
- 统计查询对 `amount_cents` 使用整数求和。
- 仅在展示层格式化为人民币字符串。

### 7.2 日期

- `accounting_date` 决定账单属于哪一天。
- `occurred_at` 只负责同日排序和具体时间展示。
- 周统计按周一至周日计算。
- 月和年使用左闭右开日期区间，避免月底边界错误。
- 自定义范围同时包含用户选择的开始和结束日期。
- 日期范围对象需同时提供“总天数”和“已过天数”：包含今天的范围两者不等，日均和环比一律使用后者。
- 判断范围是否走完所依据的“今天”必须可注入，否则日期口径无法脱离运行时钟断言。

## 8. 图片存储方案

### 8.1 目录

```text
ApplicationSupport/
  media/
    {transactionId}/
      {imageId}.jpg
      {imageId}_thumb.jpg
```

不把图片存入 SQLite BLOB，也不长期引用系统相册原始路径。

### 8.2 处理流程

```text
拍照或选择图片
→ 复制到应用临时目录
→ 校验格式和输入大小
→ 修正 EXIF 方向
→ 重新编码并清除 EXIF/GPS
→ 生成压缩大图和缩略图
→ 写入最终应用目录
→ 在数据库事务中写入账单和图片记录
→ 清理临时文件
```

建议参数：

- 每笔最多 5 张图片，上限由 `kMaxTransactionImages` 收口。
- 输入单张最大 20 MB。
- 大图最长边 2048 px。
- JPEG 质量约 85。
- 缩略图最长边 320 px。
- 文件使用 UUID 命名。
- HEIC 等输入格式统一转换为 JPEG。

### 8.3 数据库与文件一致性

SQLite 事务不能覆盖文件系统，因此保存流程必须补偿失败：

1. 先在临时目录完成图片处理。
2. 将文件复制到最终目录。
3. 开启数据库事务，保存账单及图片记录。
4. 数据库失败时删除本次新增文件。
5. 编辑账单时先提交新状态，再清理被移除的旧文件。
6. 应用启动后可低频扫描并清理没有数据库引用的孤儿图片。

删除账单时，数据库通过外键级联删除图片记录，业务层随后删除账单对应的图片目录。删除失败时记录清理任务，不能让 UI 假装文件已经清理完成。

## 9. 查询与统计

统计必须由 SQLite 聚合，禁止读取全部账单后在 Dart 内求和。

### 9.1 汇总

在指定日期范围内使用条件聚合计算：

- 总收入。
- 总支出。
- 净收支。
- 收入和支出笔数。
- 日均支出，除数取范围内已经过去的天数。

### 9.2 趋势

- 周和月按 `accounting_date` 分组。
- 年按 `accounting_date` 的月份分组。
- 自定义范围由 StatisticsRepository 决定按天或按月分组。
- 没有账单的日期或月份由 Repository 补零，图表层只负责展示。

### 9.3 分类

按 `type` 和分类根节点分组：一级分类直接使用自身，二级分类通过
`parent_id` 归集到所属一级分类。查询返回一级分类名称、图标、金额、
笔数和占比计算所需数据；账单明细仍显示实际选择的二级分类。

### 9.4 环比

对照区间由日期范围对象统一派生：范围已经走完时取完整的上一周期，仍在进行中时
只取上一周期等长的前段，长度夹在上一周期自身长度内。汇总环比与分类环比必须使用
同一个对照区间，否则同一页上两处涨跌幅会互相矛盾。

分类环比用一条查询同时聚合本期与对照期，而不是分别查两次再在内存中合并：
只出现在单侧的分类必须以“新增”或“已归零”的形式出现在结果中，分两次查询时
这类分类要靠调用方补齐键，容易漏掉一侧。归集口径与 9.3 完全一致。

明细、汇总、趋势和分类查询必须共享统一的日期范围对象，避免时间边界口径不同。

## 10. 备份与恢复

### 10.1 为什么不直接复制 SQLite

运行中的 SQLite 可能使用 WAL，并且未来数据库结构会升级。直接复制单个数据库文件容易得到不完整快照，也不利于跨版本恢复。因此 MVP 使用版本化逻辑备份。

### 10.2 备份内容

```text
backup-content/
  manifest.json
  data.json
  images/
    {transactionId}/
      {imageId}.jpg
      {imageId}_thumb.jpg
```

`manifest.json` 至少包含：

```json
{
  "format": "ligy-tally-backup",
  "version": 1,
  "createdAt": "2026-08-08T15:30:00+08:00",
  "transactionCount": 0,
  "categoryCount": 0,
  "imageCount": 0
}
```

`data.json` 保存账单、分类、图片元信息和设置。图片路径必须使用备份包内相对路径。

### 10.3 导出流程

1. 创建临时备份目录。
2. 从数据库读取一致的数据快照。
3. 生成 `manifest.json` 和 `data.json`。
4. 复制所有被引用的图片。
5. 计算文件校验信息。
6. 压缩并按产品决定进行加密。
7. 通过系统分享面板导出。
8. 删除临时目录。

备份扩展名建议使用 `.ligytally`，内部格式不要依赖扩展名判断，必须校验文件头和 manifest。

### 10.4 恢复流程

1. 用户选择备份文件。
2. 解密并解压到临时目录。
3. 校验格式版本、JSON 结构、数量和图片完整性。
4. 在临时位置创建新的 SQLite 数据库。
5. 通过数据库事务导入分类、账单、图片元信息和设置。
6. 校验新数据库统计结果和引用关系。
7. 关闭当前数据库连接。
8. 使用同一文件系统内的目录重命名方式切换数据库和媒体目录。
9. 切换失败时恢复原数据库和媒体目录。
10. 成功后清理旧数据和临时文件。

MVP 只支持覆盖恢复，不实现重复识别、合并和冲突解决。

### 10.5 加密

备份包含财务数据和图片。建议使用经过审计的实现，将压缩包通过 AES-256-GCM 进行认证加密，并使用适合口令的 KDF 派生密钥。

禁止自行设计加密算法，也禁止使用安全性较弱的传统 ZIP 密码方案。备份密码是否强制设置仍需产品确认。

## 11. 权限与隐私

- 相机权限只在用户主动拍照时申请。
- 优先使用系统照片选择器，避免申请整个相册的长期读取权限。
- 图片重新编码，移除 GPS 和其他不需要的元数据。
- 数据和媒体只放在应用私有目录。
- 不接入广告、分析 SDK 或与核心功能无关的第三方服务。
- 日志不能输出账单备注、金额、图片路径或备份密码。

## 12. 错误处理

需要定义并向 UI 映射以下错误：

- 金额或表单校验失败。
- 数据库写入失败。
- 图片格式不支持。
- 图片解码或压缩失败。
- 本地存储空间不足。
- 相机或相册权限被拒绝。
- 备份文件损坏。
- 备份版本不支持。
- 备份密码错误。
- 恢复中断或目录切换失败。

错误信息应说明结果和下一步操作，不能只展示“操作失败”。

## 13. 测试方案

### 13.1 单元测试

- 金额输入和整数分转换。
- 日、周、月、年及自定义日期边界。
- 跨时区后业务日期保持不变。
- 收入、支出、净收支和日均支出计算。
- 进行中的范围与已结束的范围各自的对照区间。
- 分类停用规则。
- 备份 manifest 和版本兼容判断。

### 13.2 数据库测试

- 全新建库和默认分类初始化。
- 每个 schema 版本的升级迁移。
- 外键和级联删除。
- 10000 笔账单的查询与统计。
- 趋势和分类金额之和与总金额一致。

### 13.3 图片测试

- 拍照、相册、HEIC、横图和竖图。
- 超大图片压缩和内存占用。
- EXIF/GPS 清除。
- 编辑和删除后的文件清理。
- 数据库失败时新增文件回滚。
- 启动孤儿文件清理。

### 13.4 备份恢复测试

- 空数据和大量数据备份。
- 无图片和多图片备份。
- 正确密码、错误密码和损坏文件。
- 空间不足和恢复中断。
- 在全新安装中恢复并核对账单、统计和图片。
- 旧版本备份恢复到新版本应用。

### 13.5 集成测试

- 新增账单后明细和统计同步更新。
- 编辑日期、类型和分类后统计迁移正确。
- 删除账单后统计和图片同时更新。
- 完整换机流程模拟。

## 14. 实施顺序

1. 创建 Flutter 工程、主题和基础导航。
2. 实现 Drift 数据库、迁移和默认分类。
3. 实现账单新增、编辑、删除和明细列表。
4. 实现筛选、搜索和日期范围模型。
5. 实现汇总、趋势和分类统计。
6. 实现图片选择、压缩、缩略图和文件一致性处理。
7. 实现 CSV 导出。
8. 实现完整备份、加密和原子恢复。
9. 完成性能、权限、异常和迁移测试。
10. 分别进行 iOS 和 Android 真机验证及发布配置。

## 15. 主要风险

### 15.1 图片导致存储增长

通过压缩、缩略图、数量限制和设置页空间统计控制。不能只限制输入文件大小，因为高分辨率图片解码仍可能产生较大内存占用。

### 15.2 数据库和文件不一致

通过临时目录、补偿删除、启动检查和恢复时整体切换降低风险。不能把 SQLite 事务误认为能覆盖文件系统。

### 15.3 备份看似成功但无法恢复

导出时生成校验信息，并为备份恢复建立自动化往返测试。备份功能的验收标准必须是“能够恢复”，而不是“能够生成文件”。

### 15.4 两台设备产生分叉

产品需要明确说明：导入是换机迁移，不是同步。迁移完成后继续使用旧设备产生的新账单不会自动出现在新设备。

## 16. 开发前待确认

1. 首发平台与发布顺序。
2. 每笔图片数量上限。
3. 备份密码是否强制。
4. 应用正式名称和 Bundle ID/Application ID。
5. 自定义统计切换聚合粒度的阈值。
