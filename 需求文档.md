# Ligy Tally 需求文档

## 1. 文档信息

- 产品名称：Ligy Tally
- 产品形态：手机端本地记账应用
- 文档状态：MVP 草案
- 更新日期：2026-08-08

## 2. 产品定位

Ligy Tally 是一款面向个人及亲友使用的轻量记账应用，帮助用户快速记录每天的收入和支出，并按日、周、月、年及自定义时间范围查看收支统计。

应用采用本地优先方案，不要求注册账号，不依赖网络，不提供云同步。账单、分类和图片均保存在设备的应用私有目录中，换机通过完整备份文件完成一次性迁移。

### 2.1 核心目标

1. 用户可以在 10 秒内完成一笔常规记账。
2. 用户可以清楚查看任意时间范围内的收入、支出和净收支。
3. 用户可以为账单添加图片，并在账单详情中查看。
4. 用户可以导出包含图片的完整备份，并在新设备恢复。
5. 应用在完全断网时仍可使用全部核心功能。

### 2.2 非目标

Ligy Tally 不是资产管理、银行账户管理或多人协作系统。MVP 不追踪银行卡、现金等账户余额，也不提供持续的多设备同步。

## 3. 已确认的产品约束

- 手机端应用。
- 纯本地存储，无业务服务端。
- 无注册、登录和账号体系。
- 单用户、单账本。
- 无账户余额。
- 支持收入和支出。
- 支持账单图片，图片保存在应用私有目录。
- 支持完整导出、备份和恢复。
- 支持日、周、月、年和自定义时间范围统计。
- 默认货币为人民币，MVP 不支持多币种。

## 4. 用户与使用场景

### 4.1 目标用户

- 开发者本人。
- 家人及朋友。
- 希望简单记账、不需要复杂财务功能的个人用户。

### 4.2 核心场景

1. 消费后快速记录金额、分类和备注。
2. 收到工资、奖金或其他收入后记录收入。
3. 为账单拍摄或选择小票、商品等图片。
4. 查看今天、本周、本月或本年的消费情况。
5. 按分类查看某段时间内的钱花在了哪里。
6. 换手机前导出完整备份，在新手机恢复。

## 5. 信息架构

应用底部设置三个主入口：

1. **明细**：账单列表、时间切换、筛选、搜索和新增账单。
2. **统计**：收支汇总、时间趋势和分类统计。
3. **设置**：分类管理、数据导出、备份恢复和应用信息。

新增和编辑账单使用独立页面或底部弹层，不占用底部导航入口。

## 6. 功能需求

### 6.1 新增账单

每笔账单包含以下字段：

| 字段 | 必填 | 规则 |
| --- | --- | --- |
| 类型 | 是 | 支出或收入，默认支出 |
| 金额 | 是 | 大于 0，最多两位小数 |
| 分类 | 是 | 收入和支出使用各自的分类 |
| 记账日期 | 是 | 默认今天，允许补记历史账单 |
| 具体时间 | 否 | 默认当前时间 |
| 备注 | 否 | 最多 200 个字符 |
| 图片 | 否 | MVP 暂定每笔最多 3 张 |

交互要求：

- 进入新增页面后直接聚焦金额输入。
- 使用数字键盘输入金额。
- 保存后立即返回明细页并更新统计结果。
- 图片支持调用相机拍摄或从系统相册选择。
- 图片上传前展示预览，允许移除后再保存。
- 图片处理失败不能破坏账单文字数据。

### 6.2 编辑与删除账单

- 用户可以修改账单的全部字段。
- 用户可以增加或删除账单图片。
- 删除账单前必须二次确认。
- 删除账单时必须同时删除该账单的本地图片和缩略图。
- MVP 直接删除数据，不提供回收站。

### 6.3 账单明细

- 默认展示当前月份的账单。
- 账单按日期分组、按时间倒序排列。
- 每日分组展示当日总收入、总支出和净收支。
- 单条账单展示分类、备注、时间、金额和图片标识。
- 有图片的账单可显示第一张图片的缩略图。
- 点击账单进入详情或编辑页面。
- 支持切换前一个或后一个月份。
- 支持按收入、支出和分类筛选。
- 支持搜索账单备注。

### 6.4 分类管理

应用内置一套通用的两级分类配置，保证首次安装即可记账。内置配置不是
任何用户现有账本的复制品，不包含个人分类数据。

用户可以：

- 新建一级或二级分类。
- 修改分类名称和图标。
- 调整分类顺序。
- 停用分类。
- 删除未被账单使用的分类。
- 导入并覆盖当前可用分类配置。
- 导出当前可用分类配置。

业务规则：

- 收入分类不能用于支出，支出分类不能用于收入。
- 已经被账单使用的分类不能直接删除，只能停用。
- 停用分类不影响历史账单展示。
- 支持一级、二级分类，不支持三级分类和标签。
- 两级交互参考钱迹的父子分类特性，但默认分类内容独立维护。
- 账单可以直接选择一级分类，也可以选择其下的二级分类。
- 已被历史账单引用的分类不能物理删除，只能停用。
- 覆盖导入配置时，历史账单引用的旧分类保留但停用，不影响历史明细。

分类配置使用版本化 JSON，结构按收入、支出和子分类嵌套表达，不依赖
设备内部数据库 ID。用户可以导出当前配置作为模板，再编辑或导入自己的配置。

### 6.5 收支统计

统计时间维度：

- 日：查看某一天。
- 周：周一至周日。
- 月：自然月。
- 年：自然年。
- 自定义：用户选择开始和结束日期。

每个时间范围展示：

- 总收入。
- 总支出。
- 净收支，即收入减支出。
- 记账笔数。
- 日均支出。

趋势统计：

- 周视图按天分组。
- 月视图按天分组。
- 年视图按月分组。
- 自定义范围较短时按天分组，较长时按月分组。
- 日视图 MVP 仅展示当日汇总和明细，不强制按小时绘图。

分类统计：

- 支出和收入分别统计。
- 展示分类金额、占比和笔数。
- 按金额从高到低排列。
- 点击分类可查看对应时间范围内的账单明细。

统计口径要求：

- 明细汇总、趋势统计和分类统计必须使用同一套查询口径。
- 趋势分组金额之和必须等于当前时间范围总金额。
- 分类金额之和必须等于当前类型的总金额。
- “净收支”不能命名为“账户余额”。

### 6.6 图片

- 图片长期保存在应用私有目录，不保存在系统相册。
- 数据库只保存相对路径和图片元信息，不保存图片二进制。
- 图片需修正方向并清除 EXIF、GPS 等隐私信息。
- 原图压缩后保存，最长边暂定 2048 px，JPEG 质量暂定 85。
- 同时生成最长边约 320 px 的缩略图。
- 账单列表只加载缩略图，查看大图时才加载压缩后的完整图片。
- MVP 不支持视频、GIF 和 Live Photo。

### 6.7 导出、备份与恢复

#### CSV 导出

- 用户可以按时间范围导出账单 CSV。
- CSV 包含类型、金额、分类、日期、时间、备注和图片数量。
- CSV 不嵌入图片。
- 中文、金额和日期必须能被常见表格软件正确读取。

#### 完整备份

- 完整备份包含账单、分类、设置及全部账单图片。
- 备份文件带格式版本、创建时间、记录数量、图片数量和校验信息。
- 用户可以通过系统分享面板保存到文件、网盘、AirDrop 或其他应用。
- 备份文件应支持加密，具体密码交互在开发前确认。

#### 恢复

- 用户选择备份文件后，应用先展示备份时间、账单数量和图片数量。
- MVP 只支持覆盖恢复，不支持合并。
- 恢复前必须明确提示当前数据将被替换。
- 恢复必须先在临时目录完整校验，成功后再替换当前数据。
- 任何恢复失败都不能破坏当前数据库和图片。

### 6.8 设置

- 分类管理。
- CSV 导出。
- 完整备份与恢复。
- 查看应用占用空间。
- 查看应用版本和隐私说明。
- 默认货币展示为人民币，MVP 不提供切换入口。

## 7. 核心业务链路

### 7.1 记账链路

```text
选择收入或支出
→ 输入金额
→ 选择分类
→ 选择日期和时间
→ 填写备注并选择图片
→ 处理图片
→ 保存账单和图片记录
→ 刷新明细及当前统计
```

### 7.2 换机链路

```text
旧手机导出完整备份
→ 通过系统能力传输备份文件
→ 新手机安装 Ligy Tally
→ 选择并校验备份
→ 覆盖恢复账单、分类、设置和图片
```

这属于一次性迁移，不属于持续的多设备同步。迁移后不应继续在两台设备分别记账，否则数据会分叉。

## 8. 非功能需求

### 8.1 性能

- 10000 笔账单下，月度明细应能正常滚动和筛选。
- 列表不得直接解码大图。
- 统计使用数据库聚合，不加载全部账单到内存计算。
- 新增普通账单的本地保存应在用户可感知的短时间内完成。

### 8.2 数据安全

- 数据库和图片只能保存在应用私有目录。
- 不申请不必要的相册长期访问权限。
- 图片重新编码后清除位置等元信息。
- 备份文件包含敏感财务和图片数据，导出时必须提示用户妥善保管。

### 8.3 可靠性

- 数据库使用版本化迁移。
- 保存账单失败时不能遗留不可见图片。
- 删除失败时应保持数据库和图片状态可恢复。
- 应用启动时可进行轻量孤儿图片检查。
- 存储空间不足时给出明确错误，不生成半成品备份。

### 8.4 可用性

- 核心功能全部支持离线使用。
- 明细、统计和编辑页面使用一致的金额与日期格式。
- 空数据、加载、错误和存储不足状态必须有明确反馈。

## 9. MVP 不包含

- 账号、登录和用户资料。
- 云同步和多设备实时同步。
- 多账本。
- 银行卡、现金等账户余额。
- 转账、报销、借贷和分期。
- 预算。
- 周期账单和自动记账。
- 多币种。
- 标签和三级以上分类。
- OCR、小票识别和银行账单导入。
- 多人共享账本。
- 同比、环比、预测和复杂财务报表。
- 桌面端和网页端。

## 10. MVP 验收标准

1. 可以新增、编辑和删除收入或支出。
2. 可以为一笔账单添加、查看和删除图片。
3. 重启应用后账单和图片仍然存在。
4. 可以按月份浏览并按类型、分类筛选。
5. 可以通过备注搜索账单。
6. 日、周、月、年和自定义统计结果准确。
7. 趋势、分类与明细汇总金额完全一致。
8. 停用分类不影响历史账单。
9. CSV 导出的金额、日期和中文内容正确。
10. 完整备份可以在全新安装的应用中恢复账单和图片。
11. 备份损坏、密码错误或存储不足时，现有数据不受影响。
12. 飞行模式下可以使用除系统文件分享外的全部核心功能。

## 11. 待确认事项

以下事项尚未最终确认，不能在实现时自行假定：

1. 首发平台是 iOS、Android，还是双端同时发布。
2. 每笔账单最多允许 1 张还是 3 张图片；当前方案按 3 张设计。
3. 完整备份是否强制设置密码，还是提供可选加密。
4. 自定义统计按天切换到按月聚合的区间阈值。
5. 应用正式名称最终使用 `Ligy Tally`、`Ligy` 还是其他名称。
